# 外观修复 + 房间统一化 实施计划

> 本文档 = 完整计划 + 续做指引。四个问题：房子内外颜色不统一、玻璃穿出屋顶、
> 某窗窗框缺结构、房间尺寸不一。已与用户确认：**全部房间统一为 7×7×3（m）**。
>
> **状态（2026-08-04）：四个问题全部完成并验收通过。** 实际修复要点：
> - 问题 1：`split_house.py` 加 `REPAINT_INTERIOR = False` 开关，停用 4 个
>   内侧面改色步骤，重跑 Blender 管线（Windows 本机用
>   `"C:\Program Files\Blender Foundation\Blender 5.1\blender.exe" -b ... --python ...`，
>   `tools/blender.sh` 是 Linux 便携版包装）。
> - 问题 2：`add_shell_core.mjs` 玻璃生成改为"簇 → u 向分列（逐窗）→ v 向
>   分段（分层）"，不再用整簇外接矩形；12 面大玻璃 → 26 面逐窗玻璃。
> - 问题 3：根因是 `add_walkable.py` 楼梯井 VOID 盒 y 上限 5.05 吃掉了北墙
>   中厅左窗(x≈-1.1)的拱顶格栅（y 4.94..4.97）；改为 4.9 后原格栅保留。
> - 问题 4：12 房间全部 7×7×3，家具重摆；`js/main.js` 室内相机碰撞只算
>   结构件（WALLS/CEILING/FLOOR/FRAMES/DOOR_*），家具不再误收缩机位；
>   `roomScene()` 机位改为南墙角高位俯拍对角，一屏看全房间。
> - 验收：`check_room_glb.py` ×12 PASS、`test-nav-rooms.mjs` 全 PASS、
>   `test-nav-real.mjs`（房子导航回归）全 PASS、`shot-all-rooms.mjs`
>   E2E 全 PASS，外壳多角度截图确认。

## 背景（现有管线）

- **房子外观**：`models_src/house.blend` →(Blender) `tools/split_house.py`（连通块分类 + 平涂着色）→ `fill_gaps.py` → `add_walkable.py` → `add_door.py` → `models/house.glb` →(Node) `tools/add_shell_core.mjs`（从 WINDOW_01 格栅自动聚类生成 `GLASS_windows` 玻璃 + `CORE_black` 黑内胆 + 门传送标记）。`add_shell_core.mjs` 幂等：首跑把原 glb 备份为 `models/house.nocore.glb`，之后一律从备份重写。
- **房间**：纯 Node 程序化生成，无需 Blender。`tools/make_room_living.mjs`（客厅）+ `tools/make_rooms.mjs`（其余 11 间，规格表驱动）。现状尺寸不一：客厅 6×5、厨房 7×5、学习室 6×6、卧室 5×4.5、卫生间 2.5×2.5、游戏室 6×5 / 5×4.5。机位/落点/光照在 `js/config.js`（`roomScene()` 模板 + `SCENES` 注册表）。
- **验收工具**：`python3 tools/check_room_glb.py <glb>`、`node tools/test-nav-rooms.mjs`、`node tools/e2e/shot-all-rooms.mjs <baseUrl>`、截图 `tools/shot.mjs`、`node tools/probe_glb_windows.mjs`。

---

## 问题 1：房子只看外观，内外统一颜色

现状：`split_house.py` 有 4 个"朝屋内的面改刷米白 MAT_wall"的面级重刷步骤
（`adjust_interior_faces` / `repaint_ground_floor` / `repaint_interior_trim` /
`repaint_attic_roof`），是旧室内时代的产物。现在室内是独立场景、旧内饰被
黑内胆封死，这些改色只在外观留下难看白块。

**改法**：
1. 验证 `tools/blender.sh` 可用。
2. `split_house.py` 的 `main()` 去掉这 4 个重刷调用，每个连通块保持分类原色
   （屋顶全红、地板全木色、木架全深棕）。
3. 重跑 Blender 管线四步（split_house → fill_gaps → add_walkable → add_door），
   删除被污染的 `models/house.nocore.glb`，重跑 `node tools/add_shell_core.mjs`。
4. 截图验证外观无突兀白块。
5. 备用方案（Blender 不可用时）：Node GLB 后处理，按法线把屋顶/地板朝内面
   从 MAT_wall 改回 MAT_roof / MAT_floor。

## 问题 2：有窗玻璃是"大方块"，从房顶伸出去

成因：`add_shell_core.mjs` 把 WINDOW_01 格栅连通块按墙面平面聚类，面内 bbox
间隙 ≤0.35m 就并成一面玻璃矩形，四周再外扩 0.08m。山墙（人字屋顶区）分散的
窗格被并成一个超出屋顶轮廓的大矩形。

**改法**：
1. `node tools/probe_glb_windows.mjs` + 截图定位出界玻璃（预期在山墙/老虎窗区）。
2. 修聚类：收紧 `GAP_2D`；并把玻璃裁剪/分段到格栅件实际覆盖范围，不再用
   单纯外接矩形填充。收敛标准：每面玻璃 bbox 不超出其成员格栅的实际覆盖。
3. 重跑 `node tools/add_shell_core.mjs`，截图确认无玻璃穿出屋顶。

## 问题 3：有一个窗户的窗框缺了一部分结构

嫌疑点（按可能性）：
- `split_house.py` 的 `pick_redundant_vbars()`（密排竖梁隔一根删一根）误删窗梃；
- `classify()` 把窗梃归错类；
- 源模型本身缺件。

**改法**：
1. 多角度截图（`tools/shot.mjs` / `tools/e2e/shot-shell.mjs`）逐面墙定位缺结构的窗。
2. `probe_glb_windows.mjs` 对照连通块确认原因，针对性修：
   删除规则加排除条件 / 调分类规则 / 参照对称窗镜像补件（仿 `mirror_ribs()`）。
3. 重跑管线 + 截图验证。

## 问题 4：全部房间统一为 7×7×3 方形

已与用户确认：12 个房间（含客厅）统一 **w=7, d=7, h=3**。房间坐标约定不变：
原点在主门口地板中心，x ∈ [-3.5, 3.5]，z ∈ [0, 7]，门窗只开南(z=0)/北(z=7)墙，
门洞宽 1.0 高 2.1，门传送连接关系（scene id / spawn id）不变。

**改法**：
1. `tools/make_rooms.mjs`：11 间规格表全部改 `w:7, d:7, h:3`，各房间 `furnish()`
   家具坐标按 7×7 重摆（家具靠墙散布、避开门洞摆动弧和落点）：
   - 厨房：北墙台面 x -3.3..1.5 / z 6.1..6.9（避开北门洞 x1.7..2.7），冰箱东墙，
     餐桌居中偏西，盆栽西南角
   - 客卫/二楼卫生间×3：马桶东北角、洗手盆北墙、镜子/淋浴隔断西墙
   - 学习室：书桌东墙、书柜西墙、地毯居中、盆栽东北角
   - 卧室×3：床西墙（床头北端，避开北门洞 x-2..-1 摆动区）、衣柜东墙、
     书桌西南、地毯居中
   - 游戏室A：电视东墙、沙发朝东、游戏架西墙；游戏室B：桌上足球居中偏西、
     箱子东北、沙发北墙西段
2. `tools/make_room_living.mjs`：客厅 `W/D/H` 改 7/7/3，南墙三门洞
   （-1.8/0/1.8）、北墙楼梯门洞 x=1.3、北墙双窗位置不变，家具 z 坐标随
   北墙 z=7 后移。
3. `js/config.js`：
   - `ROOM_SCENES` 全部改 `w:7, d:7, h:3`；`spN(x, 7)`、`winN(wx, 7)` 传参更新；
     卧室 `fromBath` 落点 z 改 6.1。
   - 客厅 `LIVING_ZONES` 两机位、`fromStudy` 落点 z=6.1、窗光/顶灯位置按
     7×7×3 调整。
   - `roomScene()` 机位模板本身按 w/d/h 参数化（45° 俯看），统一尺寸后各房间
     视角一致；确认 `maxDist = max(w,d)*1.1 = 7.7` 能看全 7×7 对角（9.9m），
     必要时微调。
4. 重新生成 + 验收：
   ```bash
   node tools/make_room_living.mjs && node tools/make_rooms.mjs
   python3 tools/check_room_glb.py models/room_living.glb   # 及各房间
   node tools/test-nav-rooms.mjs
   node tools/e2e/shot-all-rooms.mjs <baseUrl>
   ```
   逐房截图确认：全景可见、家具不挡门、落点可走。

## 实施顺序

1. 问题 4（纯 Node，最独立）
2. 问题 1（Blender 管线，为问题 3 重建基础）
3. 问题 3（定位 + 修）
4. 问题 2（只改 `add_shell_core.mjs`）
5. 全部截图回归；更新 `doc/blender-workflow-instructions.md` 第七节房间规范
   （尺寸约定变为统一 7×7×3）

每步完成后截图给用户确认再进入下一步。
