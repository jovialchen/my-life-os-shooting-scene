# 美观 / 窗户对应 / 全房间导航 改进计划（0805）

> 针对三个项目级问题：**丑（要手绘风三渲二）**、**室内外窗户不对应 + 没有房间地图**、**主页不能直达所有房间**。
> 跨设备续做用：先读「当前进度」，从下一个待办阶段继续。

## 当前进度（0805 全部完成 ✅）

- ✅ **阶段 1**：`tools/survey_windows.mjs`（外壳窗测绘，复用 add_shell_core 聚类逻辑）→ `temp/window_survey.json`；房间↔外壳窗对应表 + 三层平面图写入 `doc/house-map.md`（仅内部）。
  **注意**：学习室/卧室3 的映射实施时修订过（5 连窗与房内门洞冲突弃用 W6/W7；学习室→W8 F2 层 1 窗，卧室3→W11+W13 南墙 2 窗），以 house-map.md 现行表格为准。
- ✅ **阶段 4**：`js/systems/roomNav.js`（左上角"🏠 房间"面板，4 分组 13 场景直达，400ms 轮询高亮/锁定）；`index.html` 加 `#room-nav` 样式；`main.js` 接入 + `__app.getActiveScene`；E2E `tools/e2e/shot-roomnav.mjs` 16/16 PASS。
- ✅ **阶段 2.1**：`make_rooms.mjs`/`make_room_living.mjs` 窗口改规格表驱动（centers/width/y0/y1/arch），**台阶拱窗**（起拱线 y1-0.3，两级收缩 ×0.7/×0.35，与外壳同语汇）；卫生间×4 无窗（`winless`：spot 0、顶灯 2.2、`lamp.min 0.8` 常开——timeOfDay 新增 lampMin 下限）；config 各房 winLight 按新窗位更新。
- ✅ **阶段 2.2**：阁楼人字坡顶（spec.gable={eave:1.6,ridge:3.2}，pushGableWallX 扫描线三角墙 + pushQuad 双面坡面；洞口可从矩形段延续进三角段）；阁楼机位压低（roomScene 新增 zonePos/zoneTarget）。
- ✅ **阶段 2.3**：实体楼梯——客厅北墙 9 步（k≥7 悬空切片跨楼梯门，spawn [1.3,6.1] 在悬步下；净空规则 y>CHAR_HEIGHT 不算障碍）、学习室东墙 8 步；带阶梯挡板。
- ✅ **阶段 2.5**：check_room_glb.py 加 VIEW_EXPECT 窗位断言 + STAIRS/GABLE 存在性断言，×12 PASS；shot-lighting 窗光/顶灯位姿断言同步修正。
- ✅ **阶段 3.1**：toon 暗部 112、描边 3.0/1.5、曝光 1.15；**室内 ambient 提到 1.1（winless 1.15）、AMBIENT_LIGHT_COLOR 转暖白 0xf7efdf、室内 fill 压到 0.25**（timeOfDay 新增 profile.fill——蓝色补光是把墙照成脏灰的元凶）。
- ✅ **阶段 3.2**：`tools/room_palette.mjs` 统一结构色板（PALETTE/BASE_MATS），两个房间生成器接入；各房特色 accents 保留。
- ⏭ 可选未做：3.4 手绘贴图/纸纹后处理（当前观感已达标，后续要更"手绘"再做）。
- 文档：`doc/house-map.md`（内部布局图）、`doc/blender-workflow-instructions.md` §7.5 房间规范更新。
- 调试用截图脚本：`tools/e2e/shot_scenes.mjs <sceneId...>`（?scene= 直开截图到 temp/chk_<scene>.png）。

## 问题拆解与现状

### 问题 1：丑 —— 要手绘风三渲二

现状：管线方向已定（`doc/npr-pipeline.md`：Three.js 侧做），代码层面 `applyToonShading`（Standard→MeshToonMaterial）+ OutlinePass 描边已上线（`js/systems/toon.js`、`js/main.js:263`）。**丑的根源不在渲染器，在素材**：

- 12 间房是 `tools/make_rooms.mjs` 程序化生成的**纯色盒子**：墙/家具全是大平涂色块、无笔触、无细节比例变化，家具全是直角长方体
- 调色板各房间各自为政，没有统一色板
- gradientMap 色阶、描边粗细/颜色没按"手绘"目标调过
- 室外房子/岛是 Blender 写实比例素材，与室内盒子风格割裂

**结论：可以三渲二成手绘风，且不需要换管线**——要做的是「素材手绘化 + 渲染参数风格化」两件事。

### 问题 2：窗户不对应 + 没有地图

现状：

- 外壳 `house.glb` 有 4 组真实窗位（`tools/add_shell_core.mjs` 聚类结果）：北墙 z=-5.006、两翼前立面 z=3.969、中庭凹槽里墙 z=0.972、山墙 z=4.956
- 12 间房是**通用 7×7 模板**，窗只开在南/北墙，位置/形状/数量与外壳窗户完全脱钩；阁楼山墙尖窗在外壳上存在，房间内却没有对应
- 项目里没有任何"哪间屋子在房子的哪里"的图纸

要产出两份东西：

1. **房间布局图**（`doc/house-map.md` + 一张 SVG/ASCII 平面图）：每层哪些房间、在哪个方位、门朝哪、窗朝哪
2. **窗户对齐**：每间房的内窗位置/尺寸/形状与外壳窗洞一一对应（同一扇窗，外面看到=里面看到）

### 问题 3：主页不能直达所有房间

现状：只能在场景间走门（相邻跳转），UI 只有时间/季节滑块、语言球、指南针、机位按钮（`js/ui.js`、`js/systems/cameraZones.js`）。

要做：一个**房间导航面板**（按钮列表或小地图），列出全部 13 个场景，点击直接 `switchTo`，任意房间直达任意房间。

## 已定设计决策

| 问题 | 决策 |
|---|---|
| 手绘风在哪做 | 不换管线：Three.js toon 保留，重点改素材（贴图/色板）+ 渲染参数 |
| 房间尺寸 | **全部房间保持同尺寸**（7×7 平面不变），切换房间时人物走动体验一致，不重摆户型 |
| 阁楼造型 | 顶楼两间游戏室要能看出是阁楼：**人字坡屋顶**（平面仍 7×7，天花板沿屋脊起坡，檐口压低）+ 山墙尖窗 |
| 楼梯 | 现状楼梯只是"楼梯门"传送点，看不到实体——本轮在**客厅**（↔学习室）和**学习室**（↔阁楼）加**实体楼梯模型**（靠墙，作为障碍进导航），让人看得出"楼梯在这间屋" |
| 窗户分配 | 按房间功能布局：卧室/客厅/厨房/学习室/游戏室有窗，**卫生间可以无窗**（无窗房去掉窗景片，光照纯靠顶灯+环境光） |
| 家具 | **本轮不优化家具造型/布置**（用户后续自己细细布置）；美化只动渲染参数、色板、墙/地/顶和窗户 |
| 窗户对不上的房间 | 外壳缺窗就补窗（add_shell_core.mjs 加窗洞/玻璃），房内多窗就删——以**平面图为准**双向对齐 |
| 地图给谁看 | **仅内部开发用**（doc/house-map.md 图纸 + 对应表），**不放在页面上给用户看**；不做小地图 UI |
| 直达导航是否保留门 | 保留门动线不动，导航面板是**额外捷径**，不删任何现有交互 |

## 分阶段实施

### 阶段 1：测绘 + 房间布局图（先做"地图"，后面所有对齐以它为准）

**1.1 外壳窗户测绘**

- 扩展 `tools/probe_glb_windows.mjs`（已有，查 WINDOW_01 格栅连通块），输出**窗户清单表**：每扇窗的墙（北墙/西翼立面/东翼立面/凹槽/山墙）、世界坐标 bbox、宽高、窗台高度
- 产物：`temp/window_survey.json` + 打印表格

**1.2 制定房间→房屋区域映射 + 窗户分配**

- 根据外壳格局（两翼 + 中厅 + 凹槽门廊 + 阁楼山墙）给 13 个场景指派位置，例如：
  - 一楼：西翼=客厅、东翼=厨房+餐厅、中厅后段=客卫
  - 二楼：西翼=卧室1+卫生间1、中厅=学习室、东翼=卧室2/3+卫生间2/3
  - 阁楼：西山墙=游戏室A、东山墙=游戏室B
- **窗户按功能分配**：卧室/客厅/厨房/学习室/游戏室分到有窗的外墙；卫生间×4 无窗（内墙房）。
  无窗房：生成器去掉窗洞/窗景片，config 的 `winLight` 去掉，光照改顶灯为主
- 有窗房每间标注：门朝向、对应的**外壳窗**（来自 1.1 清单，可 1/多扇）、山墙房标尖窗

**1.3 写地图文档（仅内部）**

- `doc/house-map.md`：楼层平面图（ASCII 或内嵌 SVG，三层各一张），标房间名/门/窗/**楼梯位置**（客厅↔学习室、学习室↔阁楼两组）；附「房间↔外壳窗对应表」
- 这份文档**只给开发者看，不上页面**；它是阶段 2 改代码的唯一依据

验证：表格人工目检 + `node tools/probe_glb_windows.mjs` 输出与 temp 旧截图（shell_*.png）比对无矛盾。

### 阶段 2：房间造型对齐（窗户 + 阁楼坡顶 + 实体楼梯）

**2.1 房间生成器参数化窗口**

- `tools/make_rooms.mjs` + `tools/make_room_living.mjs`：窗口从"模板默认"改为**规格表驱动**——每房一组窗洞（墙、中心 x、宽、高、窗台高、形状），数据来自阶段 1 对应表
- **无窗房（卫生间×4）**：不开窗洞、不放窗景片 MAT_window_view；config 对应场景去掉 `winLight`、提高顶灯/环境光补偿
- 新增窗型：**山墙尖窗**（三角/拱形窗洞 + 窗棂），给阁楼两间
- 有窗房的窗景片 MAT_window_view 同步移位，窗光 `winLight` 配置（config.js）按新窗位更新

**2.2 阁楼坡屋顶（游戏室A/B）**

- `make_rooms.mjs` 给阁楼两间加**人字坡顶**：平面仍 7×7 不变，天花板改坡面——檐口高约 1.6m、屋脊高约 3.2m，东西山墙到顶（尖窗就开在山墙上）
- 坡面/山墙与平顶房同样自动成为障碍；检查坡顶不压 spawn 与可走区（檐口低区靠家具侧，不挡动线）
- config 阁楼场景适配：机位高度/俯仰按坡顶重调（别穿出屋面）、顶灯 LAMP 改挂屋脊下沿、`winLight` 对齐山墙尖窗

**2.3 实体楼梯（客厅 + 学习室）**

- 现状"楼梯"只是门上传送 extras，房间里看不到——本轮加**实体楼梯模型**：
  - 客厅：北墙楼梯门（x1.3）旁加一段靠墙直跑楼梯（通向二楼），不标属性=自动障碍
  - 学习室：加第二段楼梯（通向阁楼），位置与阁楼A的 fromStudy 落点对上
- 楼梯只到天花板封口（上去仍走门传送，不做真爬楼），踏步/扶手用盒体组合，低面数程序化
- **导航验证**：楼梯是障碍，重跑 test-nav-rooms 确认所有 spawn 落点不被楼梯/扶手卡住、双向寻路仍通（落点冲突就挪落点，不改楼梯位）

**2.4 外壳补/删窗（双向对齐）**

- 按对应表检查外壳：房间有窗但外壳对应墙面无窗 → `add_shell_core.mjs` 补玻璃面片；外壳有窗但房内不该有 → 评估封掉或调整房间映射（回阶段 1 修订）
- 保持 `CORE_black` 不凸出、玻璃厚度的既有坑位（见 plan-scenes.md 阶段 2）

**2.5 校验**

- `check_room_glb.py` 加窗口断言（窗洞中心/宽度与规格表一致）+ 阁楼坡顶节点存在性 + 楼梯节点存在性
- `tools/e2e/shot-rooms-settled.mjs` / 新增截图脚本：每房窗位截图 + 阁楼坡顶/山墙尖窗截图 + 楼梯两房截图 + 外壳四角截图目检"同一扇窗内外一致"

验证：`test-nav-real` / `test-nav-rooms` 全绿；check_room_glb ×12 PASS；smoke-app / shot-all-rooms PASS；截图目检。

### 阶段 3：手绘风美化（"丑"的主战场）

按"先参数、后素材"顺序做，每步截图对比（前后对比图存 temp/）：

**3.1 渲染参数风格化（半天工作量，收益最大）**

- `js/systems/toon.js`：gradientMap 改为 2~3 阶硬切（最动漫）；色温偏暖
- 描边调参：`OUTLINE_STRENGTH/THICKNESS/COLOR`（config.js）试"深棕粗线"与"有色描边"（暗部同色系而非统一色）两版，截图对比定稿
- `TONE_MAPPING_EXPOSURE` / TIME_PRESETS 的 HSL：整体提亮、降饱和向水彩靠
- E2E：`shot-lighting.mjs` 时段截图复跑，确认六时段不破

**3.2 统一色板**

- 全项目一份色板常量（墙/地/顶/木/布艺各 3~5 色，莫兰迪/吉卜力系），`make_rooms.mjs` 与 `add_shell_core.mjs` 的平涂色全部取自色板
- 12 房间的墙/地/顶配色按色板重出，告别各自为政；**家具保持现状不动**（用户后续自行布置）

**3.3 手绘贴图（可选加分项，最后做）**

- 程序化生成水彩/笔触纹理（Node canvas 或小脚本出 PNG：地板木纹、墙面微斑），接到 MeshToonMaterial.map；只贴墙/地/顶，不动家具
- 加**纸纹/噪点全屏后处理 pass**（极轻量 fragment pass），统一"手绘纸面"观感
- 风险：贴图风格翻车概率高，3.1~3.2 做完已达标可跳过

验证：每小步 `?frames=600` 截图前后对比目检；全测试回归（同阶段 2 命令）；mem-switch PASS。

### 阶段 4：全房间直达导航

**4.1 导航面板 UI**

- 新增 `js/systems/roomNav.js`：DOM 面板（右下角或顶部按钮"🏠 房间"展开），按楼层分组列出全部 13 场景（名取 SCENES 的 name/nameEn，跟随 `currentLang()` + LANG_CHANGE_EVENT 重刷）
- 点击 → `switchTo(sceneId, 'default')`；当前场景高亮/禁点；切换中（`isTransitioning`）锁按钮
- `index.html` 加面板样式（沿用现有 time-bar/lang-globe 风格）
- `main.js` 接入；`__app` 暴露便于 E2E

**4.2 E2E**

- 扩展 `tools/e2e/smoke-app.mjs` 或新增 `shot-roomnav.mjs`：断言 13 个按钮渲染、逐一点击直达（落点=spawn default、门图正确）、当前场景高亮、语言切换文案正确

验证：smoke-app / shot-roomnav PASS；shot-all-rooms（门动线）零回归。

## 阶段顺序与依赖

```
阶段 1（测绘+地图，仅内部）──► 阶段 2（窗户对齐）
阶段 3（美化）      独立，可与 2 并行，但建议 2 先行（窗位定了截图才稳定）
阶段 4（直达面板）   独立，随时可做
```

建议顺序：**1 → 4 → 2 → 3**（先解决"没法逛"，再解决"对不上"，最后解决"丑"——美化放最后是因为窗户/造型变了截图要重拍）。

## 关键风险与对策

- **外壳补窗穿帮**：补玻璃必须复用 add_shell_core 的"埋进窗框 0.08、厚 0.07"既有参数，先跑 shot-shell 截图目检再合入
- **房间映射与外壳格局矛盾**（如某房按映射该有两扇北窗但外壳只有一扇）：以平面图修订映射，不硬改；改动回写 doc/house-map.md
- **美化推翻重来**：3.1 参数调优先做、截图确认方向对了再动素材；每小步可独立回滚（git 工作区分开提交）
- **直达面板破坏门状态**：面板只调 `switchTo`，门开合状态恢复走既有 `_doorState` 机制，不动 doors.js 内部
- **阁楼坡顶压动线/穿机位**：檐口低区只放在家具侧，spawn 与可走区全在脊下高区；机位重调后用 shot-rooms-settled 截图确认不穿屋面
- **楼梯挡 spawn**：楼梯定位后第一时间跑 test-nav-rooms，落点冲突挪落点不挪楼梯

## 验收总标准

- 外观：六时段截图（室外全景 + 客厅 + 卧室 + 阁楼）有统一手绘观感——硬切色阶、清晰描边、统一色板（家具本轮不动，非验收项）
- 房间尺寸：13 个场景房间平面全部保持 7×7 同尺寸，切换时人物走动体验一致
- 阁楼：游戏室A/B 为人字坡顶，室内一眼看出是阁楼；山墙尖窗采光，机位不穿屋面
- 楼梯：客厅、学习室各有实体楼梯模型且不影响寻路；doc/house-map.md 图标出两组楼梯位置
- 窗户：`doc/house-map.md` 对应表 100% 落实，任一有窗房间的窗与外壳窗位置/形状一致；卫生间无窗且夜间顶灯照明正常
- 导航：主页一键直达 13 场景任一个，语言切换正常，门动线零回归；页面不出现房间地图（地图仅内部文档）
- 测试：test-nav-real / test-nav-room / test-nav-rooms / check_room_glb ×12 / check_island_glb 全绿；smoke-app / shot-room / shot-lighting / shot-all-rooms / mem-switch / shot-roomnav 全 PASS

## 常用命令速查

沿用 plan-scenes.md「常用命令速查」，新增：

```bash
# 外壳窗户测绘（阶段 1）
node tools/probe_glb_windows.mjs
# 房间生成器重跑（阶段 2 改窗后）
node tools/make_room_living.mjs && node tools/make_rooms.mjs
# 外壳补窗后重写（阶段 2）
node tools/add_shell_core.mjs
# 直达导航 E2E（阶段 4，先起 python3 -m http.server 8130）
PUPPETEER_CACHE_DIR=$PWD/tools/e2e/.cache node tools/e2e/shot-roomnav.mjs "http://127.0.0.1:8130"
```
